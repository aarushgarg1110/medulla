# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Coverage
.coverage
htmlcov/

# DB (local runtime state, not versioned)
*.db
*.db-shm
*.db-wal

# uv / python version pin (note: .python-version IS committed — pins to 3.12 for Zscaler SSL compat)
sanity-output/

# Notes and reflections
notes/
reflections/

# Eval sets are private (real queries + session ids from your own memory).
# Only the synthetic eval_set.example.json is committed.
eval_set.json
*.eval_set.json

# Claude Code local config + instructions (personal, not for publish)
.claude/
CLAUDE.md
